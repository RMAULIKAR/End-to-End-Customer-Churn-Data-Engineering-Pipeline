# End-to-End-Customer-Churn-Data-Engineering-Pipeline
An end-to-end layered data pipeline that supports churn model training and incremental batch scoring using MySQL and Python, built from a Data Engineering perspective.  The system implements structured data modeling (raw → cleaned → feature), logical data freshness through SQL views, and separation of training and inference workflows.
